spring:
  application:
    name: Produtos Api
  datasource:
    url: jdbc:h2:mem:produtos
    username: André
    password: Andrezinho
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enable: true
      path: /h2-console